# Plan, don't build

Plan this and stop. Do not write code.

GOAL: <describe it>

Produce a task list where each task has:
- a **self-contained** instruction — the worker sees nothing else: not this
  conversation, not the goal, not another task's output
- the **exact files** it creates or edits
- its dependencies, if any are real

Two tasks that run at the same time must not touch the same file. If you cannot
write a task's file boundary down, it is not a separate task — merge it.

Then show me the list and wait. Do not run anything yet.
