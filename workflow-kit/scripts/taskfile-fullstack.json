{
  "tasks": [
    {
      "id": "backend-scaffold",
      "prompt": "CREATE backend/ in this project. Task: Scaffold a Node.js Express + TypeScript REST API project. Files to create: backend/package.json (type:module, deps: express, cors, dotenv, jsonwebtoken, bcryptjs, zustand requires none; only what's listed; devDeps: typescript, tsx, @types/*), backend/tsconfig.json (strict, NodeNext), backend/.env.example (PORT, DATABASE_URL, JWT_SECRET, JWT_REFRESH_SECRET, CORS_ORIGIN), backend/.env (copy of example), backend/prisma/schema.prisma (provider postgresql; models User{id,email,passwordHash,createdAt}, Task{id,title,description?,status enum PENDING|IN_PROGRESS|DONE,userId FK,createdAt}), backend/src/lib/config.ts (dotenv loading + typed config getters), backend/src/lib/prisma.ts (PrismaClient singleton). Do NOT create any auth routes, no express app wiring yet, no middleware. Match existing style if any. Verify: bash -n on none needed; report files created + interface contract.",
      "category": "coding",
      "agent": "opencode",
      "deps": []
    },
    {
      "id": "backend-auth",
      "prompt": "CREATE backend/src/modules/auth/ and backend/src/middleware/ in this project. Task: JWT auth module. Files: backend/src/middleware/auth.ts (Bearer token parse, verify JWT, attach req.userId, 401 on missing/invalid), backend/src/modules/auth/routes.ts (Express Router mounting /register /login /me), backend/src/modules/auth/register.ts (validate email+password>=8, bcrypt hash, create DB User via prisma, return {token, refreshToken} 201), backend/src/modules/auth/login.ts (verify credentials, sign access+refresh JWTs, return both), backend/src/modules/auth/me.ts (return user from req.userId minus passwordHash). Use config from backend/src/lib/config.ts and prisma from backend/src/lib/prisma.ts (already exist). Accept: duplicate email -> 409. Do NOT touch backend/src/index.ts, do NOT scaffold docker, do NOT edit existing lib files. Verify: npx tsc --noEmit in backend/ compiles.",
      "category": "coding",
      "agent": "opencode",
      "deps": [
        "backend-scaffold"
      ]
    },
    {
      "id": "backend-api",
      "prompt": "CREATE backend/src/index.ts and backend/src/routes/index.ts in this project. Task: wire the Express app + task CRUD API. Files: backend/src/index.ts (express(), cors(CORS_ORIGIN), json(), mount /api/auth from modules/auth/routes and /api/tasks from routes/index, listen on PORT), backend/src/routes/index.ts (Express Router: GET /api/tasks -> list current user's tasks ordered createdAt desc; POST /api/tasks -> create {title,description} owned by req.userId; PATCH /api/tasks/:id -> update title/description/status, only if owned; DELETE /api/tasks/:id). All task routes PROTECTED by middleware/auth.ts (already exists). Use prisma from lib/prisma.ts, config from lib/config.ts (already exist). Do NOT modify backend/src/lib/* or modules/auth/*, do NOT add docker. Verify: npx tsc --noEmit in backend/ compiles.",
      "category": "coding",
      "agent": "opencode",
      "deps": [
        "backend-scaffold"
      ]
    },
    {
      "id": "frontend-scaffold",
      "prompt": "CREATE frontend/ in this project. Task: scaffold React + Vite + TypeScript + Tailwind. Files: frontend/package.json (deps: react, react-dom, react-router-dom, zustand; devDeps: typescript, vite, @vitejs/plugin-react, tailwindcss, postcss, autoprefixer), frontend/tsconfig.json (strict, bundler), frontend/tsconfig.node.json, frontend/vite.config.ts (react plugin, server.port 3000, proxy /api -> http://localhost:4000), frontend/tailwind.config.js (content: ./src/**/*.{ts,tsx}), frontend/postcss.config.js (tailwindcss, autoprefixer), frontend/index.html (root div + ./src/main.tsx). Do NOT create src/ components or pages yet, do NOT create backend files. Verify: files created; report exact export names/config keys.",
      "category": "coding",
      "agent": "opencode",
      "deps": []
    },
    {
      "id": "frontend-auth",
      "prompt": "CREATE frontend/src/lib/types.ts, frontend/src/lib/api.ts, frontend/src/stores/authStore.ts, frontend/src/pages/Login.tsx, frontend/src/pages/Register.tsx, frontend/src/components/ProtectedRoute.tsx in this project. Task: auth plumbing for a React app. types.ts: User{id,email}, AuthResponse{token,refreshToken}, Task{id,title,description,status,createdAt}. api.ts: fetch wrapper with baseUrl '' (relative, prod on /api), endpoints login(email,password), register(email,password), me(token). authStore.ts: zustand store {user, token, login(), register(), logout(), hydrate()} persisting token to localStorage. Login.tsx/Register.tsx: forms calling store, redirect to / on success, link to the other page. ProtectedRoute.tsx: redirect to /login when no token. Match existing scaffolding under frontend/ (vite config, index.html exist). Do NOT touch main.tsx, App.tsx, Dashboard. Verify: report exact component export names for integration.",
      "category": "coding",
      "agent": "opencode",
      "deps": [
        "frontend-scaffold"
      ]
    },
    {
      "id": "frontend-dashboard",
      "prompt": "CREATE frontend/src/pages/Dashboard.tsx, frontend/src/components/TaskList.tsx, frontend/src/components/TaskCard.tsx, frontend/src/components/Sidebar.tsx, frontend/src/App.tsx, frontend/src/main.tsx, frontend/src/index.css in this project. Task: the task-manager UI. TaskList.tsx: fetches /api/tasks with auth token, renders list of TaskCard, create form (title+description), delete + status toggle (done). TaskCard.tsx: card showing title/description/status with done checkbox + delete button. Sidebar.tsx: app name + current user email + logout button. Dashboard.tsx: layout = Sidebar + TaskList. App.tsx: react-router routes: / -> ProtectedRoute(Dashboard), /login, /register. main.tsx: ReactDOM root + BrowserRouter + index.css import. index.css: tailwind directives (@tailwind base; components; utilities). Import authStore from stores/authStore.ts and types from lib/types (already exist). Do NOT modify backend/. Verify: report that imports match inventory + tailwind.",
      "category": "coding",
      "agent": "opencode",
      "deps": [
        "frontend-scaffold",
        "frontend-auth"
      ]
    }
  ]
}
